interface:
  display_name: "Commit"
  short_description: "Create a focused local git commit"
  default_prompt: "Use $commit to inspect the scoped changes, stage only the intended files, and create a local commit without pushing."
